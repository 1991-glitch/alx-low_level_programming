#include <stdio.h>
#include <string.h>
#include "dog.h"
#include "main.h"

/**
 *print_dog - function prints struct dog
 *
 *@name: Dog's name
 *@age: Dog's age
 *@owner: Dog's owner
 *
 * Return: 0
 */
{
void print_dog(struct dog *d)
	
	if (d == NULL)

	if (d->name == NULL)
	{
		printf("nil");
	}
	if (d->owner == NULL)
	{
		printf("nil");
	}

{
	printf("Name: %s \n", d->name);
	printf("Age: %d\n", d-> age)'
	printf("Owner: %s\n", d-> owner);
}
